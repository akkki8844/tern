# tern
Tern is a GitHub App for TypeScript repositories. When an API provider’s OpenAPI specification changes, it detects breaking changes, scans an installed customer repository for affected API usages, creates a safe migration branch, runs the project’s tests, and opens a pull request with an explanation
